#include <bits/stdc++.h>
using namespace std;

//basically max non overlapping intervals

//overlapping does not happen at same end and start points...somewhere in between

//the problem is inverse of N meetings in a room
//ans=n-(total number of meetings in a room possible)