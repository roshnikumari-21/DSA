use vectors when u have to store freq of ascii(256) characters---->fastest, uses constant complexity

map-->gives O(logn) complexity for insertion and deletion, but uses more space than vectors


deque---->tc=O(1) for insertion and deletion at both ends, but uses more space than vectors


bigO--->for worst case______>this is what we have to answer
theta---->average complexity
omega---->best case complexity(lowest bound)

space complexity=> auxiliary space+input space
//never manipulate given input data 

set--->log(n)--->for insertion and deletion, 

reverse---->O(N)


recursion-->stack space consider


The time complexity of generating all permutations of a collection of n elements is:
O(N*N!)

std::map is a Red-Black Tree, so operations are O(log n)

summation of all adjacent nodes=summation of degress=2*edges


int dx[] = {-1, -1, -1, 0, 0, 1, 1, 1}; 
int dy[] = {-1, 0, 1, -1, 1, -1, 0, 1};

try not to use global variables

to find substring within a string use find() function
seq.find(rep)!=string::npos   //if the rep is found in seq

memset(vis,0,sizeof(vis));

 string binary = bitset<32>(num).to_string();   //tc=O(32) for 32 bit integer


 isalnum(ch); // checks for digits or letters

 string(1, s[i])

 __________________________________________

 getline(input_stream, string_variable, delimiter);
eg:--

string s = "apple,banana,grape";
stringstream ss(s);
string fruit;

while (getline(ss, fruit, ',')) {
    cout << fruit << endl;
}


__________________________________________

find(arr.begin(),arr.end(),x) != arr.end()    //to find x in arr

__________________________________________


vector<int>(A.begin() + left, A.begin() + left + k);    //aisa v hota h brooo...

__________________________________________

fermat's little theorem: a^(m-1) ≡ 1 (mod m) for prime m and a not divisible by m
a ka modulo inverse w.r.t. m = (a^(m-2)) % m

________________________________________


Given an array arr[] of size n, for an element arr[i], how many subarrays include arr[i]?
(i + 1) * (n - i)      //0 based indexing
________________________________________

 stable_sort(temp.begin(),temp.end(),cmp);     //to preserve order when elements are equal

  str[0]=toupper(str[0]);      //on single character

________________________________________

binary_search(nums.begin(),nums.end(),-nums[i])          //ye v exist krta h.

@ rahul subramaniam---what a legend
________________________________________
questions to ask
# what is the input size
#



________________________________________
fill(dp.begin(),dp.end(),vector<int>(m,-1));   //to fill a 2d vector with -1